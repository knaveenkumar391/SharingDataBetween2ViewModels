<UserControl x:Class="WpfApp12ViewsInMainWindow.Views.LeftPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp12ViewsInMainWindow.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Teal">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Height="50" Width="150" Content="LeftButton1" Command="{Binding LeftPageBtnClick}"/>
        <!--<TextBlock Grid.Row="1" Text="{Binding Counter, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" FontSize="20"/>-->
    </Grid>
</UserControl>
